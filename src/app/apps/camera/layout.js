export const metadata = {
    title: "Camera | SanjayOS",
    description: "Desmos-powered calculator app of SanjayOS.",
  };

export default function CameraLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}