
export const metadata = {
    title: "Clock | SanjayOS",
    description: "Clock app of SanjayOS.",
  };

export default function ClockLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}