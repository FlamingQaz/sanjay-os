
export const metadata = {
    title: "Draw | SanjayOS",
    description: "Drawing app of SanjayOS.",
  };

export default function DrawLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}